{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Python: Calibrate Leader Arm",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/Calibrate.py",
            "args": ["--id", "leader_arm"],
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}/src"
        }
    ]
}